name: Building Atomicity Authoring

on:
  pull_request:
    branches: [master]
    types: [synchronize]

permissions:
  contents: write

jobs:
  author:
    if: github.head_ref == 'agent/building-content-occupancy-v0-1'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/building-content-occupancy-v0-1
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          version: 10.13.1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Install locked authoring dependencies
        run: pnpm install --frozen-lockfile
      - name: Apply atomicity corrections
        run: python3 tooling/finalize_building_atomicity.py
      - name: Format corrected files
        run: >-
          pnpm exec prettier --write
          packages/building-core/src/building-mutation.ts
          packages/building-core/test/building-mutation.test.ts
          apps/game/src/road-building-guard.ts
      - name: Remove temporary authoring machinery
        run: |
          rm tooling/finalize_building_atomicity.py
          rm .github/workflows/building-atomicity-authoring.yml
      - name: Commit atomicity corrections
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "fix: make Building development fail closed"
          git push origin HEAD:agent/building-content-occupancy-v0-1
