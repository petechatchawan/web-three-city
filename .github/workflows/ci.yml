name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lean:
    name: Lean CI
    runs-on: ubuntu-latest
    steps:
      - run: echo runner-started
