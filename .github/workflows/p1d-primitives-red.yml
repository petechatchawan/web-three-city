name: P1-D Primitive RED

on:
  push:
    branches:
      - feat/phase-1d-terrain-generation
    paths:
      - .github/workflows/p1d-primitives-red.yml

permissions:
  contents: write

jobs:
  red:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22.18.0
          package-manager-cache: false
      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.15.1 --activate
      - name: Install
        run: |
          pnpm install --frozen-lockfile --ignore-scripts
          pnpm rebuild esbuild
      - name: Format RED test
        run: ./node_modules/.bin/prettier --write systems/terrain/tests/generation-primitives.test.ts
      - name: Verify intended RED
        run: |
          set +e
          pnpm --filter @web-three-city/terrain exec vitest run tests/generation-primitives.test.ts 2>&1 | tee /tmp/p1d-primitives-red.log
          status=${PIPESTATUS[0]}
          set -e
          test "$status" -ne 0
          grep -E "domain/generation/(splitmix64|value-noise)|Cannot find module|Failed to load" /tmp/p1d-primitives-red.log
      - name: Commit formatted RED and remove helper
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add systems/terrain/tests/generation-primitives.test.ts
          git rm .github/workflows/p1d-primitives-red.yml
          git commit -m "test(terrain): record valid P1-D primitive RED"
          git push origin HEAD:feat/phase-1d-terrain-generation
