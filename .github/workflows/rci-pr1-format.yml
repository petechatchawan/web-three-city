name: RCI PR 1 Formatter

on:
  push:
    branches:
      - feat/rci-core-contracts-v0-1
    paths:
      - 'packages/rci-core/**'
      - 'apps/game/**'
      - '.github/workflows/rci-pr1-format.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/rci-core-contracts-v0-1
      - uses: pnpm/action-setup@v4
        with:
          version: 10.13.1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prettier --write "packages/rci-core/**/*.{ts,js}" "apps/game/src/**/*.{ts,js}" ".github/workflows/rci-pr1-format.yml"
      - name: Commit formatting
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add packages/rci-core apps/game/src .github/workflows/rci-pr1-format.yml
          git commit -m "style: format rci pr1 changes"
          git push origin HEAD:feat/rci-core-contracts-v0-1
